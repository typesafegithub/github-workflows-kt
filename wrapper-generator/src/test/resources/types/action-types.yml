# See https://github.com/krzema12/github-actions-typing
typingSpec: TYPING_SPEC
inputs:
  script:
    type: string
  github-token:
    type: string
  debug:
    type: boolean
  user-agent:
    type: string
  previews:
    type: list
    separator: ','
    listItem:
      type: string
  result-encoding:
    type: enum
    allowedValues:
    - string
    - json
# Please check those outputs's description and set a proper type. 'string' is just set by default
outputs:
  result:
    type: string
