# This file was generated using Kotlin DSL (.github/workflows/_GenerateWorkflows.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Build

on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  "check_yaml_consistency":
    runs-on: "ubuntu-latest"
    steps:
      - id: step-0
        name: Check out
        uses: actions/checkout@v3
      - id: step-1
        name: Execute script
        run: rm '.github/workflows/build.yaml' && '.github/workflows/_GenerateWorkflows.main.kts'
      - id: step-2
        name: Consistency check
        run: git diff --exit-code '.github/workflows/build.yaml'
  "build_for_UbuntuLatest":
    runs-on: "ubuntu-latest"
    needs:
      - "check_yaml_consistency"
    steps:
      - id: step-0
        name: Checkout
        uses: actions/checkout@v3
      - id: step-1
        name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: adopt
      - id: step-2
        name: Build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: build
  "build_for_Windows2022":
    runs-on: "windows-2022"
    needs:
      - "check_yaml_consistency"
    steps:
      - id: step-0
        name: Checkout
        uses: actions/checkout@v3
      - id: step-1
        name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: 11
          distribution: adopt
      - id: step-2
        name: Build
        uses: gradle/gradle-build-action@v2
        with:
          arguments: build