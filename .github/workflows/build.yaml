# This file was generated using Kotlin DSL (.github/workflows/build.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Build'
on:
  push:
    branches:
    - 'main'
  pull_request: {}
jobs:
  check_yaml_consistency:
    name: 'Check YAML consistency'
    runs-on: 'ubuntu-latest'
    steps:
    - id: 'step-0'
      name: 'Check out'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Execute script'
      run: 'rm ''.github/workflows/build.yaml'' && ''.github/workflows/build.main.kts'''
    - id: 'step-2'
      name: 'Consistency check'
      run: 'git diff --exit-code ''.github/workflows/build.yaml'''
  testing:
    runs-on: 'ubuntu-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Set up JDK'
      uses: 'actions/setup-java@v3'
      with:
        java-version: '11'
        distribution: 'zulu'
    - id: 'step-2'
      name: 'check-if-snapshot'
      uses: 'gradle/gradle-build-action@v2'
      with:
        arguments: 'setIsSnapshotFlagInGithubOutput'
    - id: 'step-3'
      name: 'check-if-snapshot-2'
      run: 'echo "is-snapshot-2=true" >> $GITHUB_OUTPUT'
    - id: 'step-4'
      run: |-
        echo 'is-snapshot: ${{ steps.step-2.outputs.is-snapshot }}'
        echo 'is-snapshot: ${{ steps.check-if-snapshot-2.outputs.is-snapshot-2 }}'
    - id: 'step-5'
      run: |-
        cat $GITHUB_OUTPUT
        echo $GITHUB_OUTPUT
    - id: 'step-6'
      run: 'echo ''It''s a snapshot!'''
      if: '${{ steps.step-2.outputs.is-snapshot == ''true'' }}'
    - id: 'step-7'
      run: 'echo ''It''s NOT a snapshot!'''
      if: '${{ steps.step-2.outputs.is-snapshot == ''false'' }}'
