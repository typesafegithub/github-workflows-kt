# This file was generated using Kotlin DSL (.github/workflows/Integration tests - long strings in parameters.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-workflows-kt

name: Integration tests - long strings in parameters
on:
  push: {}
jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
    - id: step-0
      name: 'test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} test123 ${{ github.sha }} test123 ${{ github.sha
        }} test123 ${{ github.sha }} '
      uses: actions/checkout@v3
