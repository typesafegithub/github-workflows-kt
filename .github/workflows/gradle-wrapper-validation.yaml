# This file was generated using Kotlin DSL (.github/workflows/gradle-wrapper-validation.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Validate Gradle wrapper

on:
  push:
  pull_request:

jobs:
  "check_yaml_consistency":
    runs-on: "ubuntu-latest"
    steps:
      - name: Check out
        uses: actions/checkout@v2
      - name: Install Kotlin
        run: sudo snap install --classic kotlin
      - name: Consistency check
        run: diff -u '.github/workflows/gradle-wrapper-validation.yaml' <('.github/workflows/gradle-wrapper-validation.main.kts')
  "validation":
    runs-on: "ubuntu-latest"
    needs:
      - "check_yaml_consistency"
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Validate wrapper
        uses: gradle/wrapper-validation-action@v1
