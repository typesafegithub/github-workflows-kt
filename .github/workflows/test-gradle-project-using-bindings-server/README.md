# TestProject

Testproject to reproduce issue https://github.com/typesafegithub/github-workflows-kt/issues/2197#issuecomment-3738446953

* Run `./gradlew build --refresh-dependencies` to build the application.

"actions:checkout:v4" will have no problems being resolved. But "gradle:actions__setup-gradle:v4"
