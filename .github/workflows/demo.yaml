# This file was generated using Kotlin DSL (.github/workflows/demo.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'My workflow'
on:
  pull_request: {}
jobs:
  check_yaml_consistency:
    name: 'Check YAML consistency'
    runs-on: 'ubuntu-latest'
    steps:
    - id: 'step-0'
      name: 'Check out'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Execute script'
      run: 'rm ''.github/workflows/demo.yaml'' && ''.github/workflows/demo.main.kts'''
    - id: 'step-2'
      name: 'Consistency check'
      run: 'git diff --exit-code ''.github/workflows/demo.yaml'''
  my-job:
    runs-on: 'ubuntu-latest'
    needs:
    - 'check_yaml_consistency'
    steps:
    - id: 'step-0'
      uses: 'actions/checkout@v4'
    - id: 'step-1'
      name: 'Print dates in the past (shell script as string)'
      run: "\n            today=$(date +%s)\n   \n            while IFS= read -r date; do\n                date_seconds=$(date -d \"$date\" +%s)\n                if [ $date_seconds -lt $today ]; then\n                    diff=$(( (today - date_seconds) / (60 * 60 * 24) ))\n                    echo \"$date is in the past ($diff days ago)\"\n                fi\n            done < \"dates.txt\"\n            "
    - id: 'step-2'
      name: 'Print dates in the past (inlined Kotlin)'
      run: 'GHWKT_RUN_STEP=''my-job:step-2'' .github/workflows/demo.main.kts'
