# This file was generated using Kotlin DSL (.github/workflows/Integration tests - job outputs mapping.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/krzema12/github-actions-kotlin-dsl

name: Integration tests - job outputs mapping
on:
  push: {}
jobs:
  set_output:
    runs-on: ubuntu-latest
    outputs:
      bar: ${{ steps.step-0.outputs.foo }}
      pythonVersion: ${{ steps.step-1.outputs.python-version }}
      scriptKey: ${{ steps.step-2.outputs.key }}
      scriptKey2: ${{ steps.step-2.outputs.key2 }}
      scriptResult: ${{ steps.step-2.outputs.result }}
    steps:
    - id: step-0
      name: set output
      run: echo "::set-output name=foo::baz"
    - id: step-1
      uses: actions/setup-python@v4
      with:
        python-version: 3.10
    - id: step-2
      uses: actions/github-script@v6
      with:
        script: |-
          core.setOutput("key", "value")
          core.setOutput("key2", "value2")
          return "return"
  use_output:
    runs-on: ubuntu-latest
    needs:
    - set_output
    steps:
    - id: step-0
      name: use output needs.set_output.outputs.bar
      run: echo ${{ needs.set_output.outputs.bar }}
    - id: step-1
      name: use output needs.set_output.outputs.pythonVersion
      run: echo ${{ needs.set_output.outputs.pythonVersion }}
    - id: step-2
      name: use output of script
      run: |-
        echo ${{ needs.set_output.outputs.scriptKey }}
        echo ${{ needs.set_output.outputs.scriptKey2 }}
        echo ${{ needs.set_output.outputs.scriptResult }}
