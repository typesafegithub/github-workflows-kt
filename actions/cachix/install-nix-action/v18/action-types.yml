# See https://github.com/typesafegithub/github-actions-typing
inputs:
  install_url:
    type: string
  install_options:
    type: list
    separator: "\n"
    list-item:
      type: string
  nix_path:
    type: string
  extra_nix_config:
    type: string
