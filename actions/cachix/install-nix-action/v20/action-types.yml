# See https://github.com/krzema12/github-actions-typing
inputs:
  github_access_token:
    type: string
  install_url:
    type: string
  install_options:
    type: list
    separator: "\n"
    list-item:
      type: string
  nix_path:
    type: string
  extra_nix_config:
    type: string
